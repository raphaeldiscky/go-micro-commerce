{
  "name": "graphql-gateway",
  "version": "1.0.0",
  "description": "Apollo Federation Gateway for Go Microservices",
  "private": true,
  "scripts": {
    "format": "prettier --write \"**/*.{yaml,yml,json,md}\"",
    "lint": "prettier --write \"**/*.{yaml,yml,json,md}\""
  },
  "devDependencies": {
    "prettier": "3.6.2"
  }
}
