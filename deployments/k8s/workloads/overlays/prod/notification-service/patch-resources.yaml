apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
spec:
  selector: {}
  template:
    spec:
      containers:
        - name: notification-service
          resources:
            requests:
              cpu: "150m"
              memory: "256Mi"
            limits:
              cpu: "300m"
              memory: "512Mi"
