apiVersion: apps/v1
kind: Deployment
metadata:
  name: fulfillment-service
spec:
  selector: {}
  template:
    spec:
      containers:
        - name: fulfillment-service
          resources:
            requests:
              cpu: "150m"
              memory: "256Mi"
            limits:
              cpu: "300m"
              memory: "512Mi"
