apiVersion: v1
kind: Secret
metadata:
  name: chat-service-secrets
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
  labels:
    app: chat-service
type: Opaque
stringData:
  # NOTE: Override these placeholder values in overlays (local/prod)
  # For production, use external-secrets operator or sealed-secrets
  PLACEHOLDER: "replace-with-actual-secrets"
