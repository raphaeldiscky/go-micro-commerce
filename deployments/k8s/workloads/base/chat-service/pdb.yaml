apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: chat-service
  labels:
    app: chat-service
spec:
  maxUnavailable: 1
  unhealthyPodEvictionPolicy: AlwaysAllow
  selector:
    matchLabels:
      app: chat-service
