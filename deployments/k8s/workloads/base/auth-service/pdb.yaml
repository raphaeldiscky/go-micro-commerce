apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: auth-service
  labels:
    app: auth-service
spec:
  maxUnavailable: 1
  unhealthyPodEvictionPolicy: AlwaysAllow
  selector:
    matchLabels:
      app: auth-service
    matchExpressions:
      - key: component
        operator: NotIn
        values:
          - migration
