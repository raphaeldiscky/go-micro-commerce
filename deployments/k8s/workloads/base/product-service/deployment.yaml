apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  labels:
    app: product-service
    tier: backend
    version: v1
    owner: raphaeldiscky
  annotations:
    email: raph.discky@gmail.com
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
        tier: backend
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8082"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: product-service
      nodeSelector:
        workload-type: stateless
      tolerations:
        - key: cloud.google.com/gke-spot
          value: "true"
          operator: Equal
          effect: NoSchedule
        - key: workload-type
          operator: Equal
          value: stateless
          effect: NoSchedule
      dnsConfig:
        options:
          - name: ndots
            value: "2"
      restartPolicy: Always
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: product-service
          image: ghcr.io/raphaeldiscky/go-micro-commerce/product-service:v0.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8082
              protocol: TCP
            - name: grpc
              containerPort: 50052
              protocol: TCP
          env:
            - name: APP_NAME
              value: "product-service"
            - name: APP_ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: product-service-config
                  key: APP_ENVIRONMENT
            - name: HTTP_SERVER_HOST
              value: "0.0.0.0"
            - name: HTTP_SERVER_PORT
              value: "8082"
            - name: CONSUL_ENABLED
              value: "false"
            - name: TRACING_ENABLED
              value: "true"
            - name: TRACING_SERVICE_NAME
              value: "product-service"
            - name: METRICS_ENABLED
              value: "true"
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: product-pg-app
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: product-pg-app
                  key: password
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: redis-secret
                  key: password
            - name: ASYNQ_REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: redis-secret
                  key: password
          envFrom:
            - configMapRef:
                name: product-service-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 30
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
