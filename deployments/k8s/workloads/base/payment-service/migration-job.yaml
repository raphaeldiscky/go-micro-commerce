apiVersion: batch/v1
kind: Job
metadata:
  name: payment-service-migration
  labels:
    app: payment-service
    component: migration
    tier: infrastructure
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: payment-service
        component: migration
    spec:
      restartPolicy: OnFailure
      containers:
      - name: migrate
        image: payment-service-migrations:latest
        imagePullPolicy: IfNotPresent
        command:
        - /bin/sh
        - -c
        - |
          migrate -path=/migrations \
            -database "postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=disable" \
            up
        env:
        - name: DB_HOST
          value: "payment-pg-rw.default.svc.cluster.local"
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          value: "payment_db"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: payment-pg-app
              key: username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: payment-pg-app
              key: password
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
