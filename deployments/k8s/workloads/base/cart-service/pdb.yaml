apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: cart-service
  labels:
    app: cart-service
spec:
  maxUnavailable: 1
  unhealthyPodEvictionPolicy: AlwaysAllow
  selector:
    matchLabels:
      app: cart-service
      tier: backend
