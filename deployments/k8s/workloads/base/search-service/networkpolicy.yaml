apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: search-service
  labels:
    app: search-service
spec:
  podSelector:
    matchLabels:
      app: search-service
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector: {}
      ports:
        - protocol: TCP
          port: 8087
