apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: search-service
  labels:
    app: search-service
spec:
  maxUnavailable: 1
  unhealthyPodEvictionPolicy: AlwaysAllow
  selector:
    matchLabels:
      app: search-service
