apiVersion: batch/v1
kind: Job
metadata:
  name: product-service-migration
  labels:
    app: product-service
    component: migration
    tier: infrastructure
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: product-service
        component: migration
    spec:
      restartPolicy: OnFailure
      containers:
      - name: migrate
        image: product-service-migrations:latest
        imagePullPolicy: IfNotPresent
        command:
        - migrate
        args:
        - -path=/migrations
        - -database
        - postgres://postgres:postgres@postgresql-product:5432/product_db?sslmode=disable
        - up
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
