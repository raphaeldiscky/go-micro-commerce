apiVersion: batch/v1
kind: Job
metadata:
  name: notification-service-migration
  labels:
    app: notification-service
    component: migration
    tier: infrastructure
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: notification-service
        component: migration
    spec:
      restartPolicy: OnFailure
      containers:
      - name: migrate
        image: notification-service-migrations:latest
        imagePullPolicy: IfNotPresent
        command:
        - migrate
        args:
        - -path=/migrations
        - -database
        - postgres://postgres:postgres@postgresql-notification:5432/notification_db?sslmode=disable
        - up
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
