apiVersion: batch/v1
kind: Job
metadata:
  name: fulfillment-service-migration
  labels:
    app: fulfillment-service
    component: migration
    tier: infrastructure
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: fulfillment-service
        component: migration
    spec:
      restartPolicy: OnFailure
      containers:
      - name: migrate
        image: fulfillment-service-migrations:latest
        imagePullPolicy: IfNotPresent
        command:
        - migrate
        args:
        - -path=/migrations
        - -database
        - postgres://postgres:postgres@postgresql-fulfillment:5432/fulfillment_db?sslmode=disable
        - up
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
