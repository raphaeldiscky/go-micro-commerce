apiVersion: batch/v1
kind: Job
metadata:
  name: order-service-migration
  labels:
    app: order-service
    component: migration
    tier: infrastructure
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: order-service
        component: migration
    spec:
      restartPolicy: OnFailure
      containers:
      - name: migrate
        image: order-service-migrations:latest
        imagePullPolicy: IfNotPresent
        command:
        - migrate
        args:
        - -path=/migrations
        - -database
        - postgres://postgres:postgres@postgresql-order:5432/order_db?sslmode=disable
        - up
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
