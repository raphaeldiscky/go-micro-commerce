apiVersion: v1
kind: Service
metadata:
  name: cart-service
  labels:
    app: cart-service
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: cart-service
  ports:
  - name: http
    port: 8089
    targetPort: http
    protocol: TCP
  sessionAffinity: None
