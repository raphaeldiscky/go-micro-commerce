apiVersion: batch/v1
kind: Job
metadata:
  name: auth-service-migration
  labels:
    app: auth-service
    component: migration
    tier: infrastructure
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: auth-service
        component: migration
    spec:
      restartPolicy: OnFailure
      containers:
      - name: migrate
        image: auth-service-migrations:latest
        imagePullPolicy: IfNotPresent
        command:
        - migrate
        args:
        - -path=/migrations
        - -database
        - postgres://postgres:postgres@postgresql-auth:5432/auth_db?sslmode=disable
        - up
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
