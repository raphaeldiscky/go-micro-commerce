apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: graphql

resources:
  - ../apollo-router
  - httproute.yaml
  - reference-grant.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: graphql-tier
      app.kubernetes.io/managed-by: argocd
