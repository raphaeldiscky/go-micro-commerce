apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: shared-gateway
spec:
  gatewayClassName: traefik
  listeners:
    - name: https
      protocol: HTTPS
      port: 443
      hostname: "*.discky.com"
      allowedRoutes:
        namespaces:
          from: All
      tls:
        mode: Terminate
        certificateRefs:
          - name: wildcard-tls-cert
            namespace: traefik
    - name: http
      protocol: HTTP
      port: 80
      hostname: "*.discky.com"
      allowedRoutes:
        namespaces:
          from: All
