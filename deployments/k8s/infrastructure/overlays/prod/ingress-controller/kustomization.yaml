apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: traefik

resources:
  - ../../../base/ingress-controller

patches:
  - path: patch-gateway.yaml
    target:
      kind: Gateway
      name: shared-gateway
