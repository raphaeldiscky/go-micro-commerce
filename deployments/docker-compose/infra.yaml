# Local infrastructure services
include:
  - postgres.yaml
  - redis.yaml
  - kafka.yaml
  - temporal.yaml
  - monitoring.yaml
  - security.yaml
  - dev-tools.yaml
  - api-gateway.yaml
  # - dev.yaml

networks:
  go-micro-commerce:
    name: go-micro-commerce
    external: true
